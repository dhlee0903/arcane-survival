# 프로젝트 지침 · 아케인 서바이벌

도트 그래픽 마법사 서바이벌. 아래 규칙을 따른다.

## 커밋 규칙

- **Conventional Commits** 형식: `feat: ...`, `fix: ...`, `chore: ...`, `docs: ...`
- **기능 단위로 분리 커밋** — 한 커밋은 한 가지 논리적 변경만 담는다.
- 커밋 메시지 본문은 "무엇을/왜"를 한두 문장으로 설명한다.
- **`Co-Authored-By` 트레일러를 넣지 않는다.** (Claude 등 어시스턴트 co-author 표기 금지)
- 커밋/푸시는 사용자가 요청할 때만 한다.

## 문체 · 이모지

- **이모지를 남발하지 않는다.** 장식·축하용 이모지는 AI가 쓴 티가 나고 산만하다.
  문서(README·DEVLOG)와 게임 UI 텍스트 모두 해당.
- 이모지 대신 아래를 쓴다:
  - 구조·강조: 마크다운 헤더(`##`), **굵게**, 구분선(`---`)
  - 상태·목록: 체크박스 `- [ ]` / `- [x]`, 또는 텍스트 라벨 `[예정]` `[적용]` `WIP` `TODO` `DONE` `NEW`
  - 필요할 때만 얌전한 기호: `·` `—` `→` `▸` `»` `※`, 체크가 꼭 필요하면 `✓` `✗`

## 개발 패턴

- **의존성 제로**: 런타임 라이브러리 없이 순수 웹 기술(HTML/CSS/Vanilla JS)만 사용.
- **ES 모듈 + 관심사 분리(OOP)**: 로직·렌더링·입력·상태를 파일 단위로 나눈다.
- **단방향 의존**: `config.js`를 최하단에 두고 상위 모듈이 이를 참조. 순환 의존 금지.
- 렌더러는 상태를 **읽기만** 하고 변경하지 않는다.
- 게임 로직은 초당 60스텝 고정(`STEP_MS`). 상수는 모두 "1스텝당" 값으로 적는다.

## 그래픽

- 모든 그림은 `src/sprites.js`의 문자열 픽셀맵으로 정의하고 시작할 때 **아틀라스 한 장으로 굽는다**.
  이미지 파일을 저장소에 넣지 않는다.
- 픽셀맵을 고쳤으면 `validateMaps()`가 통과하는지 확인한다(CI에서도 돈다).

  ```bash
  node -e "import('./src/sprites.js').then(m=>{const b=m.validateMaps();console.log(b.length?b.join('\n'):'maps OK')})"
  ```

## 개발 로그

- `DEVLOG.md` 한 파일로 관리한다. `개발 예정(Planned)`과 `개발 적용(Changelog)` 두 섹션.
- 기능을 구현하면 `개발 예정`의 항목을 지우고, `개발 적용`에 **날짜와 함께** 옮긴다.
- 예정/적용을 별도 파일로 쪼개지 않는다.
- 게임 변경 시 `config.js`의 `VERSION`을 올린다.

## 실행

ES 모듈은 `file://`에서 로드되지 않으므로 정적 서버로 실행한다.

```bash
npx serve .
# 또는
python -m http.server 8000
```

## 검증

기능을 바꿨으면 로컬 서버로 띄워 **콘솔 에러 0**과 핵심 플레이 동작을 실제로 확인한 뒤 커밋한다.

## 배포

`main`에 푸시하면 GitHub Actions가 GitHub Pages로 자동 배포한다.
