# 아케인 서바이벌 — 개발 로그

`개발 예정`과 `개발 적용`을 한 파일에서 관리. 구현하면 예정에서 지우고 적용에 날짜와 함께 옮긴다.

---

## 개발 예정 (Planned)

- [ ] 무기 진화(뱀서의 Evolution) — 무기 만렙 + 특정 패시브 조합으로 상위 무기 획득
- [ ] 캐릭터 선택 — 시작 무기와 능력치가 다른 마법사 여럿
- [ ] 효과음 · 배경음 (발사 · 피격 · 레벨업 · 보스 등장)
- [ ] 금화와 판 사이 영구 강화(메타 프로그레션)
- [ ] 지형 오브젝트 — 부수면 아이템이 나오는 촛대 · 상자
- [ ] 결과 화면에 빌드 요약(무기 · 패시브 목록과 처치 수)
- [ ] 난이도 선택
- [ ] 시작 시 무기 선택

---

## 개발 적용 (Changelog)

### 2026-08-10 — 도트를 2배 해상도로 다시 찍음 (v1.3)

**방식 — 크기는 그대로, 밀도만 네 배**

- 프레임마다 `art` 배율을 두고, `art: 2`인 그림은 화면에 절반 크기로 그린다.
  같은 자리를 네 배 촘촘하게 찍은 셈이라 **게임 수치는 한 줄도 건드리지 않았다**
  (같은 시드로 돌린 자동 플레이 결과가 그대로 나오는 걸로 확인했다)
- 반올림도 월드 좌표가 아니라 도트 격자 위에서 한다. 안 그러면 확대했을 때 가장자리가 떨린다
- 대칭인 그림은 왼쪽 절반만 찍어 `mirror`로 붙였다 — 찍을 양이 반이고 좌우가 어긋나지 않는다

**다시 찍은 것**

- **마법사** 16×19 → 30×39. 뾰족 모자에 계조 4단, 챙, 눈·수염이 있는 얼굴,
  주름 잡힌 로브, 지팡이를 쥔 손과 빛나는 오브
- **적** — 박쥐(귀·날개막), 슬라임(광택과 눈), 해골(눈구멍·갈비뼈·번갈아 나가는 다리),
  유령(흘러내리는 자락), 골렘(가슴 룬), 리치(왕관·이빨·로브 주름·고동치는 핵)
- **소품·바닥** — 보석 3종, 하트, 자석, 상자, 마력 화살, 서리 파편, 수호 룬, 번개, 불꽃,
  바위·풀·묘비, 바닥 타일(32×32 잡티라 결이 훨씬 곱다)
- 팔레트도 계조를 늘렸다(예: 마법사 모자 4단 · 로브 4단 · 수염 3단)

**남은 것** — 아이템 아이콘 13개는 아직 1배다. HUD 슬롯과 카드에서만 보여서 뒤로 미뤘다.

### 2026-08-10 — 디버그 콘솔 · 경험치 곡선 완화 (v1.2)

**디버그 콘솔** (`` ` `` 또는 F1)

- 마우스가 있는 기기에서만 만든다(`pointer: fine`). 폰에서는 아예 DOM에 붙지 않는다
- 읽기 — fps · 프레임/스텝 처리 시간, 논리 해상도 · 배율 · 넓이 배율, 체력 · 레벨 ·
  경험치 · 좌표, 개체 수(적 · 보석 · 투사체 · 파편 · 장판), 가진 아이템과 보정치
- 시간 — 정지 · 0.25x · 1x · 2x · 4x, 한 스텝씩 넘기기, +1분 건너뛰기
- 치트 — 무적 · 즉시 레벨업 · 무기/패시브 만렙 · 회복 · 상자
- 소환 — 보스 · 엘리트 · 떼거리 40마리 포위 · 전멸
- 보기 — 히트박스(실제 판정 원은 그림보다 작다) · 스프라이트시트
- **게임 로직에는 손대지 않았다.** 게임 쪽에 늘어난 건 무적 플래그와 시간 점프 두 개뿐이고,
  `debug.js`를 지워도 그대로 돈다. 시간 점프는 건너뛴 구간의 보스·포위가 한꺼번에
  터지지 않도록 지나간 것으로 표시한다(`Spawner.catchUp`)

**경험치 곡선 완화**

- `xpNeed`의 세 계수를 함께 낮춰 **전 구간 약 13% 감소**(1레벨 6→5, 10레벨 79→69,
  25레벨 327→283). 특정 구간만 깎으면 성장 리듬이 울퉁불퉁해진다
- 자동 플레이 기준 생존 시간 폰 6:49/11:22 → 10:20/11:21, PC 4:04/6:19 → 6:30/7:12.
  도달 레벨은 8~14에서 12~16으로 올랐다

### 2026-08-10 — 도트 확대 · PC 풀스크린 · 아이템 두 갈래 (v1.1)

**스프라이트를 키웠다**

- 그림을 다시 찍는 대신 **월드를 통째로 확대**했다. 논리 해상도를 `창 크기 ÷ ZOOM`으로
  계산하고 캔버스 변환에 배율을 곱한다 — 도트가 커지는 건 `renderer.resize()` 한 줄이다
- 작은 화면 2배, 큰 화면 3배. 마법사가 화면 폭의 5% → 8%로 커졌다

**PC에서 화면을 꽉 채운다**

- 고정 비율(320×560) 판때기를 버리고 캔버스가 창 전체를 덮는다. 화면 위 제목 헤더도
  걷어냈다(타이틀 오버레이가 이미 갖고 있다). 폰은 세로로 길고 PC는 가로로 넓다
- **보이는 넓이에 맞춰 적 밀도를 보정한다**(`areaScale`). 확대하면 보이는 세상이 좁아지는데
  소환량이 그대로면 순식간에 포위된다 — 실제로 자동 플레이 생존 시간이 4~7분에서
  1분으로 떨어졌다. 상한 · 소환량 · 포위 인원을 넓이에 비례해 줄여 4~11분으로 되돌렸다
- 조준 범위와 처진 적 재배치 거리도 화면 크기에 비례하게 바꿨다. 안 보이는 적을 쏘면 탄만 버린다
- HUD를 세 줄로 다시 앉혔다(시간 / 체력 / 레벨 · 처치). 좁은 폰에서 시간과 체력이 겹쳤다
- 설정 버튼과 GitHub 링크가 오른쪽 위에서 겹쳐서, 번갈아 보이게 했다(플레이 중엔 설정만)

**아이템을 공격 / 패시브 두 갈래로**

- 뱀서처럼 갈래를 화면에 드러냈다. 레벨업 카드에 `공격` · `패시브` 라벨이 붙고,
  HUD 슬롯은 **위가 공격 5칸 · 아래가 패시브 5칸**으로 나뉜다. 빈 칸도 그려서 앞으로
  몇 개를 더 들 수 있는지 보이게 했다
- **아이템 아이콘 13개를 새로 찍었다**(11×11 통일). 패시브는 그동안 색 사각형이라
  아이템으로 보이지 않았고, 무기는 투사체 그림을 그대로 써서 화염 오라와 불의 낙인이
  같은 불꽃으로 보였다
- 패시브 이름을 물건 이름으로 바꿨다 — 마력의 구슬 · 바람의 깃털 · 생명의 물약 ·
  시간의 모래 · 확장의 고리 · 자석 · 지혜의 서 · 재생의 물약

### 2026-08-10 — 첫 판 (v1.0)

**뱀서 시스템 차용**

- **조작은 이동뿐.** 무기는 각자의 재사용 대기시간마다 자동으로 나간다
- 적은 화면 밖 테두리에서 계속 밀려오고, 죽으면 **경험치 보석**을 떨군다
- 보석을 먹어 레벨이 오르면 **시간이 멈추고 카드 세 장** 중 하나를 고른다(무기 5칸 · 패시브 5칸 · 각 6레벨)
- 무기 6종(마력 화살 · 서리 파편 · 수호 룬 · 화염 오라 · 연쇄 번개 · 불의 낙인),
  패시브 8종(마력 · 신속 · 생명력 · 집중 · 확장 · 자석 · 지혜 · 재생)
- 시간대별 소환 구성 + 사건(포위 · 무리 · 엘리트 · 보스). 엘리트와 보스는 **상자**를 떨구고
  먹으면 무기 하나가 즉시 한 단계 오른다
- 15분 생존이 목표. 기록은 생존 시간(초)으로 남고 홈 카드와 키를 공유한다

**도트 그래픽 · 스프라이트시트**

- 모든 그림을 소스에 문자열 픽셀맵으로 적고 시작할 때 **아틀라스 한 장으로 굽는다**(40프레임).
  이미지 파일은 하나도 쓰지 않는다. F2로 구워진 시트를 화면에서 확인할 수 있다
- 리치(27×31)는 좌우 대칭이라 **왼쪽 절반 + 가운데 열**만 찍고 구울 때 뒤집어 붙였다
- 프레임마다 `lift`를 줘서 발밑 기준으로 한 도트 띄운다 — 걷기 · 날갯짓 · 부유가 이 한 칸으로 산다
- 바닥 타일만은 손으로 찍지 않고 **시드 고정 난수로 잡티를 흩뿌려** 만들었다.
  16×16 노이즈를 문자열로 적으면 읽지도 고치지도 못한다
- 굽기 전에 픽셀맵을 검사한다(`validateMaps`) — 줄 길이가 하나만 어긋나도 그림 전체가 밀리는데,
  실제로 리치를 그리다 두 번 걸렸다
- 애니메이션은 클립 표(`frames` · `hold` · `loop`)로 제어한다. 적이 수백 마리라
  개체마다 Animator를 두지 않고 자기 나이만으로 프레임을 꺼내는 `frameAt`을 쓴다

**만들면서 고친 것**

- **달아나기만 하면 무적이었다.** 적이 뒤로 늘어서기만 하고 따라붙지 못해, 대충 도망치는
  자동 플레이가 15분을 체력 100으로 넘겼다. 세 군데를 고쳤다
  - 소환 지점을 **가는 쪽에 더 많이**(60%) 세운다
  - 620px 넘게 처진 적은 버리지 않고 **앞쪽 테두리로 다시 세운다**
  - 적 이동 속도를 올려 플레이어(1.5)와의 차이를 좁혔다
- **보석이 화면 밖에 500개씩 쌓였다.** 흘리고 지나간 보석이 그대로 남아 경험치가 끊겼다.
  일정 거리 안이면 천천히 따라오게 하고, 150개를 넘으면 **가장 먼 보석을 그 다음 보석에 합친다**(총량 보존)
- **오라·룬 안에 있는 적이 흰 실루엣으로 굳었다.** 매 프레임 맞으니 피격 번쩍임이 안 꺼진 것.
  번쩍인 뒤 잠깐 쉬는 구간을 둬서 보스가 제 모습으로 보이게 했다
- 흙 타일을 칸마다 따로 뽑았더니 얼룩처럼 흩어져서, **2×2 덩어리로 뭉치게** 해 맨땅처럼 보이게 했다
- 묘비의 십자가가 十가 아니라 T로 읽혀서 새로 찍고, 배경에 묻히도록 팔레트를 낮췄다
- HUD 우측 상단 글자가 설정 버튼과 겹쳐 좌측으로 모았다
